<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>WordSauber</title>
    <script src = "https://cdn.tailwindcss.com"></script> 
</head>

<body>
    <div class="main w-full min-h-screen bg-zinc-900">
        <div class="form p-10 text-white">  
             <form action = "/create" method ="post"> <%# This works on the fronend site and leads to the /create route through post method %>
                <input class = "block w-full rounded-md px-5 py-2 bg-zinc-800" type = "text" placeholder = "Enter your title" name="title">
                <textarea class = "block w-full rounded-md mt-2 px-5 py-2 resize-none bg-zinc-800" placeholder = "What's popping ?" name="description"></textarea>
                <%# You need those title and description for referrring to the entered data%>
                <input class="mt-2 bg-blue-600 px-5 py-2 rounded-md" type = "submit" value = "Create Task">
             </form>    
        </div>
        <div class="tasks flex gap-3 flex-wrap p-10"> 
            <% if(files.length>0){ %>
               <% files.forEach(function(val){ %>
                  <div class = "task w-72 px-3 py-4 rounded-md bg-zinc-800">
                     <h1 class = "text-white text-3xl tracking-tighter"><%= val %></h1>
                     <a class ="text-blue-600 inline-block mt-3" href="/file/<%= val%>">Read More.</a>
                     <a class = "text-red-800 inline-block mt-3 px-2 py-3" href = "/edit/<%= val%>">EDIT</a>
                  </div>
                  <% }) %>
             <% } else { %>
                  <h3 class = "text-zinc-600"> NO TASKS YET. </h3>
             <% } %>
        </div>

    </div>
</body>
</html>
